# Optional: use a personal OpenWeather API key for the graded API path.
# Keep the actual key in Cloudflare Pages or a local .env.local file, never Git.
VITE_OPENWEATHER_API_KEY=

# Use this only when deploying the lab10 directory to GitHub Pages.
VITE_BASE_PATH=/
