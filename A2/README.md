# SilverCare Connect - foundation snapshot

This first snapshot implements Assignment 2 Business Requirement A.1 (Vue.js 3 development stack) and Business Requirement A.2 (responsive design). It uses Vue 3, Vite, responsive CSS and Vue-rendered service data.

```powershell
npm install
npm run dev
```
